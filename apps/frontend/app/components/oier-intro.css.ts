import { style } from '@vanilla-extract/css';

export const container = style({
  marginBottom: '1.5rem',
});

export const heading = style({
  marginBottom: '1rem',
  fontSize: '1.2rem',
  fontWeight: 600,
});
