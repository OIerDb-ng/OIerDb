import { style } from '@vanilla-extract/css';

export const container = style({
  paddingTop: '1rem',
  paddingBottom: '1rem',
});

export const name = style({
  marginBottom: '1rem',
  fontSize: '2rem',
  fontWeight: 600,
});
