import { style } from '@vanilla-extract/css';

export const container = style({
  paddingTop: '1.5rem',
  paddingBottom: '2rem',
});

export const heading = style({
  fontSize: '1.25rem',
  fontWeight: 600,
  marginBottom: '1rem',
});
